# Allow vetted dependencies to run install scripts under pnpm v10 security model
# This enables native binaries required by Astro/Vite image pipeline in CI.
only-built-dependencies=esbuild sharp

# Optional: ensure CI is detected
ci=true
